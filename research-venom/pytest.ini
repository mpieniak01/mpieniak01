[pytest]
testpaths = tests
addopts = -q
markers =
    windows_office: requires Windows Office COM; excluded from public CI
    integration: longer local integration test without Office
    network: requires real API/token access; excluded from public CI
    data_quality: local checks over current private artifacts; excluded from public CI
